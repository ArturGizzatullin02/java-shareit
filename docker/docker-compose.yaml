version: '1.0'

name: java-shareit-compose
services:
  db:
    image: postgres:16.1
    container_name: postgres_db
    ports:
      - '6432:5432'
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
      - POSTGRES_DB=shareit_db
      - TZ=Europe/Moscow
#    healthcheck:
#      test: pg_isready -q -d $$POSTGRES_DB -U $$POSTGRES_USER
#      timeout: 5s
#      interval: 5s
#      retries: 10